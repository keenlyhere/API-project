export default function NoSpots({ isLoaded }) {
    return (
        <div className="NoSpots-container">
            <h1>Sorry, there are no spots available where you are going.</h1>
        </div>
    )
}
